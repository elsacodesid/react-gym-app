import React from 'react'
import SectionWrapper from './SectionWrapper'

const Generator = () => {
  return (
    <SectionWrapper header={"generate your workout"} title={["it\'s", "Fit", "O\'Clock"]}></SectionWrapper>
  )
}

export default Generator